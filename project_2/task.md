1. Название продукта (`name`): это строка, которая хранит название товара. Например, "Apple".
2. Количество (`quantity`): это число, которое показывает, сколько единиц товара добавлено в корзину. Например, 3.
3. Цена (`price`): это число, которое показывает стоимость одной единицы товара. Например, 0.5 (за одно яблоко).
4. Дата добавления (`dateAdded`): это дата и время, когда товар был добавлен в корзину. Мы можем автоматически задавать это поле с помощью Mongoose.
   Задание 3

5. Создайте схему продукта:

В папке `models/` создайте файл `Product.js`.
Определите схему продукта с полями `name`, `quantity`, `price`, и `dateAdded`.

2. Создайте модель продукта:

На основе созданной схемы создайте модель `Product` с использованием `mongoose.model()`.
Экспортируйте модель, чтобы ее можно было использовать в других файлах вашего проекта.

Создайте маршрут `GET /api/cart`:
В папке `routes/` создайте файл `cartRoutes.js`.
В этом файле создайте маршрут для получения всех продуктов, используя метод `find` модели `Product`.
Убедитесь, что маршрут возвращает данные в формате JSON.
Проверьте работу маршрута:
Запустите сервер и перейдите по адресу `http://localhost:3333/api/cart`.
Убедитесь, что в браузере или Postman отображаются данные в формате JSON.
3.Создайте маршрут `POST /api/cart`:
Добавьте маршрут в `cartRoutes.js`.
Используйте метод `create` для добавления нового продукта в базу данных.
Убедитесь, что маршрут возвращает добавленный продукт в формате JSON.
4.Проверьте работу маршрута:
Используйте Postman или аналогичный инструмент для отправки POST-запроса на `http://localhost:5000/api/cart` с JSON данными продукта.
Убедитесь, что продукт добавляется в базу данных и возвращается в ответе.

////////////////////////////////
Задание 4

1. Создайте маршрут `GET /api/cart`:

В папке `routes/` создайте файл `cartRoutes.js`.
В этом файле создайте маршрут для получения всех продуктов, используя метод `find` модели `Product`.
Убедитесь, что маршрут возвращает данные в формате JSON.

2. Проверьте работу маршрута:

Запустите сервер и перейдите по адресу `http://localhost:5000/api/cart`.
Убедитесь, что в браузере или Postman отображаются данные в формате JSON. 3. Создайте маршрут `POST /api/cart`:

Добавьте маршрут в `cartRoutes.js`.
Используйте метод `create` для добавления нового продукта в базу данных.
Убедитесь, что маршрут возвращает добавленный продукт в формате JSON.

4. Проверьте работу маршрута:

Используйте Postman или аналогичный инструмент для отправки POST-запроса на `http://localhost:5000/api/cart` с JSON данными продукта.
Убедитесь, что продукт добавляется в базу данных и возвращается в ответе.

Задание 5

1. Создайте маршрут `PUT /api/cart/:id`:

Добавьте маршрут в `cartRoutes.js`.
Используйте метод `findByIdAndUpdate` для обновления продукта по ID.
Убедитесь, что обновленный продукт возвращается в формате JSON.

2. Проверьте работу маршрута:

Отправьте PUT-запрос на `http://localhost:5000/api/cart/:id` (замените `:id` на реальный ID продукта) с новыми данными.
Убедитесь, что данные продукта обновляются в базе данных и возвращаются в ответе. 3. Создайте маршрут `DELETE /api/cart/:id`:

Добавьте маршрут в `cartRoutes.js`.
Используйте метод `findByIdAndDelete` для удаления продукта по ID.
Убедитесь, что возвращается сообщение о успешном удалении.

4. Проверьте работу маршрута:

Отправьте DELETE-запрос на `http://localhost:5000/api/cart/:id` (замените `:id` на реальный ID продукта).
Убедитесь, что продукт удаляется из базы данных и подтверждение возвращается в формате JSON.

Задание 6

1. Убедитесь, что MongoDB запущен и подключение настроено корректно.
2. Добавьте все созданные маршруты в app.js
3. Запустите сервер командой `node app.js`.
4. Проверьте маршруты с помощью Postman или другого инструмента для тестирования API.
